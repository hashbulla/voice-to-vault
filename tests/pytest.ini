[pytest]
testpaths = tests/unit tests/integration
addopts = -v --tb=short --strict-markers
markers =
    unit: pure unit tests, zero external calls
    integration: integration tests, mocked external calls
    evaluation: LLM evaluation, real API calls, manual only
    smoke: smoke tests, real APIs + real infra, manual only
